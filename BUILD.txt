I have provided Makefiles to allow building using the nmake tool.

To build everything (core, tests, sample clients, etc.) simply open a command-prompt and type:
	
	nmake

To build tests, type:

	nmake test


When you are done building, check the bin and lib folders.

