I have provided Makefiles to allow building using the nmake tool.

To build everything (core, tests, sample clients, etc.) simple open a cmd-prompt and type:
	
	nmake

To build tests, type:

	nmake test

